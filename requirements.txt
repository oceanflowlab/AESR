# Python runtime dependencies (Python >= 3.10)
openai>=1.30.0
requests>=2.31.0
tqdm>=4.66.0
Pillow>=10.0.0

# Provider SDKs used by the full AESR pipeline
google-genai>=1.0.0
dashscope>=1.20.0
volcengine-python-sdk[ark]>=1.0.0
